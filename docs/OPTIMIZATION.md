# 灵墨优化路线图 v2

> 上一版追着竞品跑。这一版问自己：用户到底为什么用灵墨？

## 一个事实

灵墨的竞争力只有一个：**AI 写出来的东西能直接用**。

不是架构漂亮，不是部署方便，不是社区活跃。是质量控制。

竞品在架构、记忆、部署上领先我们。但没有一个在"写出来的小说质量"上超过我们。这是我们的基因，也是唯一的护城河。

所以优化方向不是补齐短板——是把自己的长处拉到更长。

---

## 优先级重排

### P0：用户现在就用不了的东西（本周必须修）

| # | 问题 | 证据 | 方案 |
|---|------|------|------|
| 1 | **服务器不稳定** | 本轮对话中后端挂了至少 5 次 | 加进程守护（systemd/launchd），或至少启动脚本加自动重启 |
| 2 | **引号修复不生效** | 生成了 3 次第 9 章，每次都有 20+ 孤儿引号。de_ai 测试能修，但服务器跑的代码不生效 | 已加了 save 前强制 de_ai，需要验证。根本原因可能是 pyc 缓存 |
| 3 | **FreeLLM token 过期** | 之前测试返回 492 错误，用户配置了也无法用 | 加 token 过期检测 + 友好提示 |

### P1：直接影响用户留存的东西（本月）

| # | 内容 | 为什么重要 | 怎么做 |
|---|------|-----------|--------|
| 4 | **降成本** | 当前每章 5-8 次 LLM 调用。DeepSeek 虽然便宜，但用户感知是"等很久" | 合并可并行的 LLM 调用（edit+spec 已合并）、把 pattern disruption 改为默认关闭（用户可选开启） |
| 5 | **补充题材** | 24→37 种，直接扩大可写范围。每个新题材 = 一组 quality_rules + StyleProfile | 对照 webnovel-writer 的 37 题材清单，补缺 |
| 6 | **章节记忆（轻量版）** | 长篇写到 50 章后 AI 开始"忘记"，这是用户最大的痛点 | 每章生成后用 1 次 LLM 调用提取事实（不用 3 层，只用 1 层），注入下一章 prompt |

### P2：让现有功能更好用（下个月）

| # | 内容 | 为什么 | 怎么做 |
|---|------|--------|--------|
| 7 | **拆分 _legacy.py** | 不改功能，只改结构。为后续开发提效，但不直接创造用户价值 | 按路由前缀拆 4 个文件，每步验证测试 |
| 8 | **拆分 generator.py** | 同上 | 提取 scorer/prompts/deai/voices |
| 9 | **前端性能** | 首次加载 100+ 组件，有优化空间 | 代码分割 + 懒加载 |

### P3：锦上添花（有时间再做）

| # | 内容 | 为什么排最后 |
|---|------|-------------|
| 10 | Docker 部署 | 降低新用户门槛，但不影响现有用户 |
| 11 | GitHub Actions CI | 自动化测试，但本地手动跑也够 |
| 12 | README 英文版 | 国际化，但不是当前优先级 |

---

## 明确不做的

| 项目 | 原因 |
|------|------|
| **Agent Loop（LLM 自主决策）** | 风险高（成本翻倍、可能不终止）、用户未要求。novel-ai 做这个是因为它们的生成管线本身弱，需要 Agent 补偿。我们的固定管线已经够好 |
| **3 层记忆完整版** | 每章额外 2+ 次 LLM 调用，成本翻倍。用户感知的提升不明确。1 层轻量版够用 |
| **知识市场** | 需要服务器 + 审核 + 用户系统，solo dev 不现实 |
| **多用户/OIDC** | 保持单用户本地工具定位，不要膨胀 |

---

## 为什么不是"追竞品"

novel-ai 做 3 层记忆 + Agent Loop 是因为他们的生成管线本身弱——一个简单 prompt 直出文本，没有质量评分、没有重试、没有自动校准。他们用架构复杂性补偿生成质量的不足。

我们的 12 步管线、8 维评审、排版修复、作家声音、自动校准——这套东西跑出来的小说质量已经很高。在这个基础上再加 Agent Loop，边际收益很低。

webnovel-writer 4.7k stars 是因为先发优势 + Claude Code 生态。我们是独立 Web 应用，不是同一个赛道。

**我们的路线是：质量控制做到极致 → 降低使用成本 → 扩大覆盖面。不是：追架构 → 追生态 → 追社区。**

---

## 本轮已完成

- [x] 12 步生成管线 + 8 维 LLM 评审（含排版和灵魂维度）
- [x] 排版自动修复（引号统一 + 孤儿引号）
- [x] 14 种作家声音 + StyleProfile 自动校准
- [x] 断点续传 + 前后端双锁防重
- [x] 错误信息友好化（技术错误 → 中文提示）
- [x] 编辑自动保存（5s debounce）+ Ctrl+S / Ctrl+Enter 快捷键
- [x] 增强导出（EPUB/PDF + 目录 + 封面 + 页码）
- [x] 大纲拖拽排序 + 关系图标签常显 + Diff 三模式
- [x] FreeLLM 集成（13 免费模型，已写入 Provider）
- [x] 新用户 3 步引导
- [x] 365 tests · 0 TS errors
- [x] 上下文窗口溢出保护 + 空内容保护 + Fallback 透明化
