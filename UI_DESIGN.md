# Novel Writer — UI/UX 设计系统

**设计方向**: Writer's Atelier（作家工坊）

---

## 1. 设计理念

这不是一个 SaaS 后台，这是一个**数字化的写作工作室**。

核心隐喻：一张老木桌、一盏台灯、一叠稿纸。工具感退后，创作感上前。每个像素都在说"写吧，这里很安全"。

### 设计原则

| 原则 | 含义 |
|------|------|
| **纸优于屏** | 暖色纸感背景，让眼睛放松，像面对稿纸而非屏幕 |
| **内容即界面** | 章节正文是最高优先级，工具按钮不抢注意力 |
| **意图可见** | 双模式切换是战略性交互，必须一眼看懂当前状态 |
| **少即是暖** | 去掉冷冰冰的卡片框线，用间距和微妙的背景区分层级 |
| **手感到位** | 按钮有按压反馈，页面切换有翻页感，编辑器打字有质感 |

---

## 2. 设计 Token

### 2.1 色彩

```
Paper（纸）          Ink（墨）            Accent（丹青）
─────────────────   ─────────────────    ─────────────────
bg:    #FBF7F0       primary: #1C1917     indigo: #3D4F7A
card:  #FFFFFF       muted:   #78716C     amber:  #C4956A
hover: #F5F0E8       subtle:  #A8A29E
border:#E6E0D4
```

### 2.2 字体

```
Headings:  "Crimson Text", Georgia, serif        → 文学感
Body/UI:   -apple-system, "PingFang SC", sans    → 清晰
Editor:    "Crimson Text", Georgia, "KaiTi", serif → 稿纸感

Size scale:  12 / 13 / 14 / 15 / 16 / 18 / 22 / 28 / 36
```

### 2.3 间距

```
4px 网格:  4 / 8 / 12 / 16 / 20 / 24 / 32 / 40 / 48 / 64 / 80
```

### 2.4 圆角

```
按钮: 6px    卡片: 8px    输入框: 6px
模式切换: 99px（胶囊形）   编辑器: 4px
```

### 2.5 阴影

```
无 box-shadow。用 border 区分层次。唯一例外：mode switcher 有微弱光晕。
```

---

## 3. 布局架构

```
┌─────────────────────────────────────────────────────────┐
│  Header (56px)                                          │
│  ┌──────┐                              ┌──────────────┐ │
│  │ Logo │                              │ Mode Toggle  │ │
│  └──────┘                              └──────────────┘ │
├───────────┬─────────────────────────────────────────────┤
│           │                                             │
│  Sidebar  │          Main Content Area                  │
│  200px    │                                             │
│           │    ┌───────────────────────────────────┐    │
│  · 工作台 │    │  Page Content                     │    │
│  · 设置   │    │                                   │    │
│  · 日志   │    │                                   │    │
│           │    └───────────────────────────────────┘    │
│           │                                             │
│  ──────── │                                             │
│  小说列表  │                                             │
│  · 修仙.. │                                             │
│  · 都市.. │                                             │
│           │                                             │
└───────────┴─────────────────────────────────────────────┘
```

关键变化：
- **Header 替代深色 Sidebar 放品牌**：更轻量、更现代
- **Sidebar 变浅色**：融入纸感背景，不放品牌信息
- **小说列表出现在侧栏**：快速切换，不需要回到 Dashboard
- **Mode Toggle 在 Header 右侧**：最战略性的控件放在最显眼的位置

---

## 4. 核心组件规格

### 4.1 Mode Toggle（模式切换器）

```
┌──────────────────────────────┐
│  ○ 全自动    ● 创作者        │   胶囊形切换
│  定时运行    人在回路         │   当前模式有 indigo 填充
└──────────────────────────────┘
```

设计要点：
- 始终可见（Header 右侧）
- 切换有 200ms 过渡动画
- 切换时页面内容平滑过渡，不跳变
- 每个模式有副标题说明当前状态

### 4.2 小说卡片

```
┌──────────────────────────────────────┐
│                                      │
│  修仙从炼丹开始                       │  ← Crimson Text, 18px
│  第 42 章 · 82,400 字                │  ← muted, 12px
│                                      │
│  一个普通药师获得上古丹方...           │  ← 2 行截断
│                                      │
│  [查看详情] [生成下一章]              │  ← 按钮组
│                                      │
└──────────────────────────────────────┘
```

- 无边框，用微妙的 `bg: #FFFFFF` 在 `bg: #FBF7F0` 上自然浮起
- hover 时卡片微微上移 2px + 增加不透明度变化
- 书名用 serif 字体，突出文学气质

### 4.3 章节编辑器

```
┌─────────────────────────────────────────────┐
│                                             │
│  输入本章方向...                             │  ← placeholder text
│  ┌─────────────────────────────────────┐    │
│  │ 主角突破金丹，但天道降下雷劫，       │    │
│  │ 同时宿敌趁机偷袭宗门...              │    │  ← 多行输入
│  └─────────────────────────────────────┘    │
│                                             │
│  [✨ 生成 3 个草稿]                         │  ← primary action
│                                             │
│  ── 或 ──                                   │  ← 视觉分隔
│                                             │
│  草稿 A · 草稿 B · 草稿 C                   │  ← 3 列卡片
│                                             │
├─────────────────────────────────────────────┤
│          第二章 · 雷劫炼金丹                  │  ← 标题
│                                             │
│  天边乌云翻滚，雷光在云层中酝酿...            │
│                                             │  ← 正文区域
│  ...(2000+ 字)...                           │     serif 字体
│                                             │     24px 行高
│  [保存]  [发布]                              │
└─────────────────────────────────────────────┘
```

设计要点：
- 编辑器正文用 serif 字体，像在稿纸上写
- 行高 1.8，舒适阅读
- 最大宽度 720px 居中，不撑满屏幕
- 工具栏浮动在底部，不遮挡内容

### 4.4 空状态

```
              📖
        还没有小说

    创建你的第一部 AI 小说
    或探索 AI 的创作可能

      [创建新小说]
```

- 图标用大号 unicode，不用 emoji
- 副标题给两个选择：实用主义 + 探索主义
- CTA 按钮是唯一强调色

### 4.5 Loading 状态

```
┌─────────────────────────────────────┐
│                                     │
│  ✧  正在构思第 43 章的剧情...       │  ← 文学化的 loading 文案
│                                     │
│  ┌─────────────────────────────┐    │
│  │ ████████████░░░░░░░░░░░░░░░ │    │  ← skeleton bar
│  └─────────────────────────────┘    │
│                                     │
│  预计还需 15 秒                      │
└─────────────────────────────────────┘
```

- Skeleton 替代 spinner
- 文案变化："正在构思..." → "正在落笔..." → "正在润色..."
- 显示预计时间

---

## 5. 动效规范

| 元素 | 动效 | 时长 | 缓动 |
|------|------|------|------|
| 页面切换 | 淡入 + 上移 8px | 200ms | ease-out |
| 卡片 hover | 上移 2px + 阴影 | 150ms | ease |
| 按钮 press | 缩放 0.97 | 100ms | ease-in |
| Mode toggle | 背景色过渡 | 200ms | ease |
| Toast 进入 | 上滑 + 淡入 | 200ms | ease-out |
| Toast 退出 | 淡出 | 150ms | ease-in |
| 草稿卡片展开 | 高度动画 | 300ms | ease-in-out |
| 编辑器加载 | 骨架屏 → 内容淡入 | 400ms | ease-out |

---

## 6. 响应式断点

```
Desktop:  ≥ 1024px    → 侧栏 200px + 内容区
Tablet:   768-1023px  → 侧栏折叠为顶部 Tab
Mobile:   < 768px     → 单列，Header 精简，Mode Toggle 缩小
```

---

## 7. 无障碍

- 所有交互元素可键盘聚焦（Tab 导航）
- Focus ring: `2px solid #3D4F7A`，`outline-offset: 2px`
- 触摸目标 ≥ 44×44px
- 色彩对比度 ≥ 4.5:1（正文）/ ≥ 3:1（大标题）
- `prefers-reduced-motion` 禁用所有动效
- `prefers-color-scheme: dark`（V2 暗色主题）

---

## 8. 页面优先级（视觉层级）

```
Dashboard:
  1. 统计概览（几本、几章、几字）
  2. 小说列表（按最近更新排序）
  3. 创建入口（次要）

Novel Detail:
  1. 模式切换器 + 当前模式状态
  2. 操作区（生成/发布按钮）
  3. 章节目录

Editor:
  1. 方向输入区 + 生成按钮
  2. 草稿选择（生成后）
  3. 正文编辑区（展开后）
  4. 保存/发布按钮
```

---

## 9. 微交互

1. **生成按钮**：点击后文字变为"正在构思..."，带 skeleton 动画
2. **草稿选择**：hover 时微微放大，选中后有 indigo 左边框
3. **字数计数**：编辑器下方实时显示字数，超过目标时变绿
4. **发布确认**：发布前弹出平台格式预览，确认后再发布
5. **Toast**：成功用微妙绿，失败用暖红，信息用 indigo
6. **模式切换时**：当前书籍的 auto/manual 状态标签同步变化
