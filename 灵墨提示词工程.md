# 灵墨 · 提示词工程文档

> 建造一个 AI 小说生成系统 + 用它写一本能在番茄小说跑一个月的书，所需全部提示词、架构决策、工作流程。

---

## 目录

1. [世界观构建](#1-世界观构建)
2. [角色灵魂系统](#2-角色灵魂系统)
3. [生成管线](#3-生成管线)
4. [前端设计系统](#4-前端设计系统)
5. [后端工程架构](#5-后端工程架构)
6. [数据库设计](#6-数据库设计)
7. [完整创作流程](#7-完整创作流程)
8. [发布策略](#8-番茄小说发布策略)
9. [踩过的坑](#9-踩过的坑)

---

## 1. 世界观构建

### 1.1 核心 Prompt

```
为一本都市高武小说构建完整世界观。

包含以下模块，每个模块限制在2-3句话：

## 力量体系
- 不要"练气→筑基→金丹"的修仙模板
- 力量和主角的日常职业绑定（如：外卖骑手的功德系统）
- 必须有硬上限和软上限，超限有具体后果
- 变强必须有代价，代价必须是情感性的（不是"折寿"这种数字）
- 用表格：境界 | 效果 | 风险

## 精算规则
- 量化力量获取（如：每送一单外卖=+1~10功德）
- 量化力量消耗（如：用功德兑换修为=-功德）
- 设计临界点机制（如：500功德触发天庭扫描）

## 三大法则
- 每条法则只说一遍，不要重复解释
- 法则之间互相制约，不能各自独立
- 每条法则都有"为什么是这个法则"的叙事理由

## 城市地理（5个区域，新手村→最终副本）
- 每个地点绑定一种妖魔类型
- 每个地点绑定一种人类情绪（焦虑/绝望/欲望）
- 地点要有"日常+异常"的双层描写

## 角色档案（至少4个）
- 表面身份 + 真实身份
- 核心创伤（不是背景故事，是驱动一切的内在伤痕）
- 台词样本（一句能体现性格的话）
- 对主角的关系：是镜像、对比还是互补
- 用表格呈现

## 黄金三章大纲
- 每章一个核心场景 + 一个反转 + 一个钩子
- 第一章结尾必须让读者立刻想看第二章
- 不超过100字/章

格式：Markdown。禁止形容词堆砌。禁止"宏大的世界""精彩的故事"等空话。
```

### 1.2 实际产出

`data/worldbuilding-delivery-god.md`（225行）：
- 功德悖论体系（6阶境界 + 精算规则 + 临界点机制）
- 外卖即献祭暗线
- 妹妹代价机制（情感锚点）
- 5区域地理 + 5大法则
- 6角色完整档案
- 黄金三章大纲

### 1.3 关键教训

- **先跑世界观再写角色**：世界观定了力量规则，力量规则定了角色的选择空间，选择空间定了冲突
- **代价必须前置**：读者需要在第1章就知道变强的代价是什么。不能第10章才说"原来每次用能力都会折寿"
- **法则不要超过5条**：超过5条读者记不住。每条法则在第一卷至少要触发一次

---

## 2. 角色灵魂系统

### 2.1 灵魂指纹（核心矛盾）

#### Prompt

```
以下是30组文学中的根本矛盾（polarities），分为存在/社会/内心/关系/时间五类。

每组包含：
- id: kebab-case标识
- emoji: 视觉符号
- name: 矛盾名称（如"自由↔命运"）
- poles: [左极, 右极]
- question: 这个矛盾逼迫角色追问的那个问题（必须是开放性的，不能有答案）
- prompt: 注入到每章生成的写作法则（写"怎么做"，不写"做什么"）
- deadlySins: 如果这样做，张力就消失了（3条，具体可操作）
- masters: 3位处理过这个矛盾的大师
  - author: 作者名
  - work: 作品名（含书名号）
  - note: 一句话说出他们怎么处理的（不要解读，要说"怎么做"）

分类标准：
- 存在（metaphysical）: 关于人为什么活着、什么是真实
- 社会（social）: 关于个人与时代、权力与无力
- 内心（inner）: 关于欲望与约束、理智与疯狂
- 关系（relational）: 关于爱恨、信任与背叛
- 时间（temporal）: 关于过去与现在、重复与新意

每组不超过150字。输出为TypeScript数组，嵌入React组件。

示例（不要照抄，仅展示格式）：
{
  id: 'freedom-fate',
  emoji: '⛓️',
  name: '自由 ↔ 命运',
  poles: ['我要选择', '我已注定'],
  question: '如果你最自由的选择——恰好印证了你逃不掉的命运——你是自由的还是被困的？',
  prompt: '每一章让角色做一个选择。但选择的后果不是他们能控制的。自由的行动导致宿命的结果。',
  deadlySins: ['命运可轻松改变', '自由选择无后果', '宿命让角色放弃努力'],
  masters: [
    {author:'金庸',work:'《天龙八部》',note:'乔峰的命运早在血统中注定'},
    {author:'索福克勒斯',work:'《俄狄浦斯王》',note:'逃避预言恰好实现预言'},
    {author:'马尔克斯',work:'《百年孤独》',note:'每一代都反抗命运也都重复命运'}
  ]
}
```

#### 交互设计

用户选择矛盾 → 1-10滑块定位立场 → 写个人回答 → 保存到数据库。

这个回答会在每章生成时注入 prompt，确保整本书不偏离核心追问。

#### 数据流

```
前端 SoulEngine → POST /api/v2/novels/{id}/soul-fingerprint → SQLite soul_fingerprints表
前端加载     ← GET  /api/v2/novels/{id}/soul-fingerprint ← localStorage兜底
生成注入     ← _gen_directions[id+"_soul"] → Generator._call_llm_with_retry
```

### 2.2 角色蓝图（金庸标准）

#### Prompt

```
设计角色系统，按金庸的标准。

每个角色必须有（缺一不可）：
- name: 姓名
- role: 主角/反派/导师/配角
- entrance: 出场方式——令人难忘的第一印象
  （黄蓉=在船上偷鸡烤鸡的乞丐少女，杨过=断臂独行的大侠）
- signature: 标志性特征——让人一眼认出的特质
  （不能是"长得帅""性格好""很强大"）
- speechPattern: 说话方式——独特的语言风格
  （韦小宝=满嘴脏话，黄蓉=机智俏皮，郭靖=憨厚短句）
- coreWound: 核心创伤——驱动一切的内在伤痕
  （不是"父母双亡"就完了，要说这个死亡让他变成了什么样的人）
- surfaceTrait: 表面性格——别人眼中的他
- hiddenSelf: 隐藏自我——只有读者知道的真实
- arcStart: 弧线起点——故事开始时他是谁
- arcEnd: 弧线终点——故事结束时他变成谁
- voiceSample: 台词样本——一句最能体现他性格的话
  （"靖哥哥"三个字就是黄蓉）

选填项：
- obsession: 执念——他反复做/想/说的一件事
- contradiction: 内在矛盾——他身上的悖论
- contrastWith: 与哪个角色形成化学反应的对比
- contrastHow: 如何形成对比

额外功能：
- 角色化学反应分析：任意两个角色之间的对比自动生成insight
- 达标检查：缺出场/台词/创伤任一即标注"不完整"
- 底部设计原则提示：金庸角色设计的三条法则

数据持久化：API先（POST /api/v2/novels/{id}/character-blueprints），localStorage兜底。
```

#### 数据流

```
前端 CharacterSoul → POST /api/v2/novels/{id}/character-blueprints → SQLite character_blueprints表
前端加载          ← GET  /api/v2/novels/{id}/character-blueprints    ← localStorage兜底
前端删除          → DELETE /api/v2/novels/{id}/character-blueprints/{charId}
```

### 2.3 系统集成

`NovelCharacters.tsx` 页面组织三层角色系统：

```
角色页面
├── CharacterSoul      — 角色蓝图编辑器（金庸标准，localStorage+API）
├── CharacterGraph     — 角色关系图（从legacy characters数据）
├── CharacterVoices    — 角色声音配置
├── SoulWorkshop       — 灵魂工坊
└── SoulEngine         — 核心矛盾选择器
```

---

## 3. 生成管线

### 3.1 管线架构

#### Prompt

```
设计一个12步AI小说生成管线，每步独立可追踪：

1. constraint_compression  — 将前文约束压缩为紧凑prompt（ConsistencyCompressor）
2. technique_advisor       — 选择叙事技巧（非对称/反差/留白等）
3. batch_generate          — 生成3个候选版本，取最优
4. llm_judge              — LLM 6维度评分（见3.3）
5. edit                    — 精修+具体化（合并self_edit+specificity）
6. de_ai                   — 正则匹配AI写作痕迹，随机替换（见3.2）
7. constraint_check        — flash模型硬约束验证（见3.4）
8. humanize                — 随机句长变化、段落打破（保护结尾钩子）
9. deslop_check            — BrainAgent检查AI废话
10. consistency_check      — 前文一致性验证
11. foreshadowing_resolve  — 自动回收伏笔
12. tts_pregen             — 后台生成语音（非阻塞）

每一步有：
- TraceRecorder记录：耗时、结果、成本
- 失败自动重试3次（指数退避）
- SSE实时推送到前端（GenerationStatusBanner渲染管道图）

质量门控：
- overall < 0.8 → 触发重写
- 连续3次不达标 → 降级通过（避免死循环）
- 降级通过时在章节上标记

技术实现：
- 主模型：从数据库providers表读取（deepseek-v4-pro）
- 备选模型：base_url不同于主模型的provider
- 流式回调：gen._on_stream_chunk推送到_gen_status["stream_content"]
- 全局上下文：章节>30时，RAG top_k从5提升到15
- 章节教训注入：读取前3章质量追踪，避免重复错误
```

### 3.2 AI痕迹检测（de_ai）

#### Prompt

```
收集中文AI写作的可检测模式，编译为Python正则表达式列表 AI_PATTERNS。

分类：
1. 句法模式
   - 连续3句以上相同句长（字数差≤2）
   - 段落长度均等（连续3段字数差≤10）
   - 以"了/的/吧/吗/呢"结尾的句子占比>40%

2. 词汇模式
   - "仿佛" "似乎" "那是一种" "宛如" "如同"
   - "在...中" "在...里" "在...下" 句首结构
   - "然而" "但是" "因此" "于是" 连续使用
   - "一种难以言喻的" "一种说不清的" "一种莫名的"

3. 结构模式
   - 开头总结句（"这是...的一天"）
   - 结尾升华句（"或许...才是..." "人生就是..."）
   - 强行前后呼应（flag + 回收太工整）

4. 标点模式
   - 同一标点连续使用>3次（如四个句号连用）
   - 感叹号密度异常（>1个/100字）
   - 省略号滥用（>3个/段）

每条pattern附带替换策略：
- 不是简单删除，是随机改写为3种变体之一
- 用 random.choice 增加变化性

_humanize_text() 实现要点：
- 随机合并相邻短段落
- 随机拆分长段落
- 保护对话段落不被合并
- **关键：保护结尾钩子**——如果最后一句以？！结尾，不能被合并/拆分
- 实现为两阶段：先检测结尾钩子，再处理正文，最后拼接钩子
```

### 3.3 LLM Judge 质量评分

#### Prompt

```
LLM Judge对章节进行6维度评分（1-10），返回结构化JSON：

{
  "hook": {"score": N, "reason": "..."},
  "pacing": {"score": N, "reason": "..."},
  "dialogue": {"score": N, "reason": "..."},
  "readability": {"score": N, "reason": "..."},
  "antagonist": {"score": N, "reason": "..."},
  "want_next": {"score": N, "reason": "..."},
  "overall": N.NN,
  "grade": "A/B/C/D",
  "biggest_issue": "..."
}

评分标准：
- A级 ≥ 0.80
- B级 ≥ 0.65
- C级 ≥ 0.50
- D级 < 0.50

关键约束：
- 不能所有维度打一样的分（防止评分通胀）
- 禁止设置常量分（如"consistency=0.85"）——这会导致平均值虚高
- 必须给出具体的biggest_issue（不能是"无"或"基本可以"）
- want_next是最重要指标——读者是否想点下一章
- 即使整体好，也必须指出一个改进点

评分prompt中增加反通胀说明：
"请严苛评分。不要因为内容'没有大问题'就给满分。
 如果只是'合格但不出彩'，给6-7分。
 只有真正让人印象深刻的内容才给8分以上。"
```

### 3.4 硬约束检查

#### Prompt

```
在每章生成后，用flash模型（低延迟）进行硬约束检查：

检查项：
- 角色OOC：角色是否做了不符合已有性格设定的事
- 力量体系漏洞：是否违反了力量体系规则
- 前文矛盾：是否与已有章节的事实冲突
- 伏笔是否在期限内回收
- 重要设定项是否被忽略（如：本章应出现但未出现的角色/地点）

返回格式：
{
  "passed": true/false,
  "violations": [
    {"type": "OOC|power|continuity|foreshadowing|missing", "detail": "..."}
  ]
}

已通过：继续后续步骤
未通过：标记violations，注入到下一轮edit的约束中
```

### 3.5 SSE 实时推送

#### 架构

```
前端:
  new EventSource(`/api/novels/${id}/generate/stream`)
  → onmessage: 解析GenStatus JSON → setGenStatus(s) → 渲染到GenerationStatusBanner

后端（legacy SSE）:
  async def generate_stream_sse(novel_id):
    while True:
      status = _get_status(novel_id)
      if changed: yield f"data: {json.dumps(status)}\n\n"
      if status in ("complete", "error"): break
      await asyncio.sleep(0.5)

后端（v2 SSE）:
  事件驱动: gen_state.get_event(novel_id)
  await asyncio.wait_for(event.wait(), timeout=30)
  → 只在状态真正变化时推送，减少空轮询

前端GenerationStatusBanner:
  四阶段管道图（构思→修复→润色→评估）
  + 进度条 + 计时器 + 完成时质量评分
  + 错误时显示详情+重试按钮
  + 不自动消失，用户手动关闭
```

---

## 4. 前端设计系统

### 4.1 设计 Token

#### Prompt

```
创建一个名为"灵墨"的AI写作工具设计系统。

### 色彩
- 基调：暖光素笺（纸张质感，不是科技感）
- Primary: #F5F0E8（素笺白）
- Ink: #2C2416（墨色）
- Accent: #B45309（暖橙，不是蓝紫）
- 暗色模式：墨色底 + 暖色字 + 降低对比度

### 字体
- 正文：Noto Serif SC（衬线，有纸质阅读感）
- UI / 数据：Inter（无衬线，现代感）
- 编辑器：等宽或衬线
- 禁止：Arial, Roboto, system fonts（太AI味）

### 动画
- 过渡：300ms ease-out
- 光标：3s呼吸周期（写作编辑器中）
- Hover：轻微放大 + 阴影过渡
- 页面进入：staggered fade-in（animation-delay递进）
- 禁止：弹跳、翻转、过度使用动画

### 组件规范（Tailwind class组合）
- Card: bg-card border border-border rounded-xl
- Button主: bg-accent text-white rounded-xl shadow-lg active:scale-95
- Button次: border border-border text-ink-muted hover:text-ink
- Progress: h-1.5/h-2 bg-border rounded-full overflow-hidden > bg-accent
- Typography: font-heading / font-editor / text-ink / text-ink-muted / text-ink-subtle
- Skeleton: animate-pulse bg-border rounded
- Input: border border-input bg-card rounded-lg focus:border-accent focus:ring-1

### 风格原则
- 90% UI消失——只留下用户正在用的
- 纸墨感——不是科技产品的冷感，是书的温感
- 极简但不简陋——空白要有力量
- 暗色模式不是反转色，是重新设计
- 永远不要紫蓝渐变——那是AI味的标志
```

### 4.2 写作编辑器（WriterView）

#### 布局

```
┌─────────────────────────────────────────────────┐
│ ← 返回  小说名                    第N章 Q:0.83 │ 顶栏 (h-10)
├──────┬──────────────────────────┬────────────────┤
│      │ [生成状态横幅]           │                │
│      │ 构思→修复→润色→评估      │                │
│ 章   │ ████████░░ 65%          │   上下文面板   │
│ 节   │                          │   ·写作约束    │
│ 列   │  第一章标题              │   ·角色状态    │
│ 表   │                          │   ·质量评分    │
│      │  正文内容...             │                │
│ 200px│                          │   300px        │
│      │                          │                │
│      │                          │                │
├──────┴──────────────────────────┴────────────────┤
│ [第4章] [🔮 生成下一章] [▼方向]                │ 底部固定栏
└─────────────────────────────────────────────────┘
```

#### 生成状态横幅（GenerationStatusBanner）

**显示时机：** 生成开始 → 持续显示 → 用户手动关闭

**三种状态：**

| 状态 | 颜色 | 内容 |
|------|------|------|
| 运行中 | 橙色边框 | 四阶段管道图 + 进度条 + 计时器 + 阶段消息 |
| 完成 | 绿色边框 | Q:等级(分数) + 消息 + **「查看新章节」按钮** |
| 失败 | 红色边框 | 错误详情 + **「重试」按钮** |

**关键交互：**
- 完成后不自动消失（之前3秒消失的bug已修复）
- 点击 ✕ 手动关闭
- 重复点击生成按钮 → toast提示"正在生成中"，不静默忽略

### 4.3 方向预设

```
7个方向标签，每个对应一种叙事需求：

打斗 → "战斗场面，动作描写，招式对决"
感情 → "情感互动，关系发展，内心独白"
反转 → "剧情反转，出人意料，打破常规"
日常 → "日常互动，角色塑造，氛围营造"
悬疑 → "悬疑氛围，线索推进，谜题展开"
伏笔 → "埋下伏笔，回收旧线，前后呼应"
高潮 → "高燃剧情，极限爆发，情绪顶点"

点击标签 = 追加到方向文本框
方向可选，空白时AI自行决定
```

---

## 5. 后端工程架构

### 5.1 分层设计

```
novel_writer/
├── server.py              # FastAPI入口，注册路由，启动时init_deps()
├── config.py              # 环境变量配置（OPENAI_API_KEY等）
├── database.py            # SQLite数据访问层（手写SQL，不用ORM）
├── generator.py           # 核心生成引擎（12步管线）
├── state.py               # 全局共享状态（GenerationState类+Lock）
├── schemas.py             # Pydantic v2类型定义
├── db_models.py           # SQLAlchemy ORM（v2 API用）
├── trace.py               # 生成管线可观测性（TraceRecorder）
├── story_state.py         # 故事状态数据类
├── log_config.py          # 日志配置
├── schema.sql             # 数据库建表语句
│
├── services/
│   ├── novel_service.py       # 小说CRUD业务逻辑
│   └── generation_service.py  # 生成任务封装
│
├── routers/
│   ├── deps.py            # 共享依赖（get_db, gen_state）
│   ├── audiobook.py       # 听书路由
│   ├── drama.py           # 短剧路由
│   ├── script.py          # 剧本路由
│   └── novel/
│       ├── __init__.py    # 组合所有子路由
│       ├── crud.py        # v2 CRUD端点（Pydantic类型安全）
│       └── _legacy.py     # 7000行原始路由（逐步迁移中）
│
└── stations/
    └── novel/
        ├── bible_extractor.py     # 故事圣经提取
        ├── consistency_scorer.py  # 一致性评分
        └── constraint_builder.py  # 约束构建
```

### 5.2 核心架构决策

| 决策 | 选择 | 原因 |
|------|------|------|
| ORM | 不用，手写SQL | 迁移成本大，零用户可见收益 |
| 前端状态管理 | useState/useContext | Zustand/Redux对当前规模过度 |
| API版本 | /api（legacy） + /api/v2（新） | 不动现有端点，渐进迁移 |
| 数据库 | SQLite | 单机部署，零运维 |
| LLM客户端 | OpenAI Python SDK | 兼容DeepSeek/MiniMax/OpenAI |
| 流式推送 | SSE（Server-Sent Events） | 单向推送，比WebSocket简单 |
| 全局状态 | GenerationState类 + Lock | 消除双重_gen_status bug |

### 5.3 状态管理

#### 问题

重构前，`_gen_status`在两个文件中各有一份独立的dict，生成状态不同步。

#### 方案

```python
# state.py
class GenerationState:
    def __init__(self):
        self._lock = threading.Lock()
        self._status: dict[str, dict] = {}
        self._directions: dict[str, str] = {}
        self._job_queue: dict[str, dict] = {}
        self._events: dict[str, threading.Event] = {}
        self._agent_memos: dict[str, list] = {}

# deps.py — server.py启动时注入单例
def init_deps(db, gen_state):
    global _db, _gen_state
    _db = db
    _gen_state = gen_state

# _legacy.py — 兼容别名
_set_status = lambda *a, **kw: _gstate().set_status(*a, **kw)
_gen_status = _gstate()._status  # 直接引用底层dict
```

### 5.4 数据库模块延迟加载

#### 问题

`database.py`模块顶层 `db = get_db()` 导致模块导入时即实例化，可能出现双实例。

#### 方案

```python
# _legacy.py
class _LazyDB:
    """延迟代理——首次访问属性时才解析真实DB实例"""
    def _real(self):
        return get_db()  # 从deps获取全局单例
    def __getattr__(self, name):
        return getattr(self._real(), name)

db = _LazyDB()  # 安全——导入时不做任何事
```

---

## 6. 数据库设计

### 6.1 核心表

```sql
-- 小说元数据 + 世界观
CREATE TABLE novels (
    id TEXT PRIMARY KEY,
    title TEXT, author TEXT, genre TEXT, synopsis TEXT,
    status TEXT DEFAULT 'draft',
    world_name TEXT, world_era TEXT, world_geo TEXT,
    power_system TEXT, world_rules TEXT,  -- JSON array
    main_arc TEXT, current_arc TEXT,
    arc_chapter_start INTEGER DEFAULT 1,
    provider_id TEXT DEFAULT 'deepseek',
    created_at TEXT, updated_at TEXT
);

-- 章节
CREATE TABLE chapters (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    novel_id TEXT NOT NULL,
    number INTEGER NOT NULL,
    title TEXT, content TEXT,
    word_count INTEGER DEFAULT 0,
    quality_score REAL DEFAULT 0,
    model_used TEXT,
    ending_hook TEXT,
    key_events TEXT, revelations TEXT,  -- JSON
    prompt_tokens INTEGER, completion_tokens INTEGER, cost REAL,
    generation_duration_ms INTEGER,
    UNIQUE(novel_id, number)
);

-- 角色（WorldEditor用）
CREATE TABLE characters (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    novel_id TEXT, char_key TEXT UNIQUE,
    name TEXT, role TEXT, personality TEXT,
    background TEXT, power_level TEXT,
    status TEXT DEFAULT 'alive',
    CHECK(role IN ('主角','反派','配角','导师','路人'))
);

-- 势力
CREATE TABLE factions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    novel_id TEXT, name TEXT, description TEXT, leader TEXT,
    sort_order INTEGER DEFAULT 0
);
```

### 6.2 AI记忆表

```sql
-- 灵魂指纹
CREATE TABLE soul_fingerprints (
    novel_id TEXT PRIMARY KEY,
    polarity TEXT, position INTEGER DEFAULT 5,
    answer TEXT,
    created_at TEXT, updated_at TEXT
);

-- 角色蓝图（CharacterSoul完整数据）
CREATE TABLE character_blueprints (
    novel_id TEXT PRIMARY KEY,
    characters_json TEXT DEFAULT '[]',  -- JSON array
    created_at TEXT, updated_at TEXT
);

-- 生成追踪
CREATE TABLE chapter_traces (
    novel_id TEXT, chapter_num INTEGER,
    trace_json TEXT,  -- 12步完整记录
    created_at TEXT
);

-- 章节版本历史
CREATE TABLE chapter_versions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    chapter_id INTEGER REFERENCES chapters(id) ON DELETE CASCADE,
    content TEXT, word_count INTEGER,
    version INTEGER DEFAULT 1,
    reason TEXT, created_at TEXT
);
```

### 6.3 安全约束

```python
# database.py
ALLOWED_NOVEL_COLS = {
    'title','author','synopsis','genre','status','mode',
    'world_name','world_era','world_geo','power_system','world_rules',
    'main_arc','current_arc','arc_chapter_start','deleted_at','provider_id'
}

def update_novel(self, novel_id, **kw):
    bad = [k for k in kw if k not in self.ALLOWED_NOVEL_COLS]
    if bad:
        raise ValueError(f"Invalid column(s): {bad}")
    # ... 参数化查询
```

### 6.4 前端兜底策略

每个用户数据都有两层存储：

```
写入: API(POST/PUT) → SQLite + localStorage.setItem()
读取: API(GET) → 成功则覆写localStorage → 失败则读localStorage
```

关键key命名：
- `soul-fingerprint-{novelId}` — 灵魂指纹
- `characters-soul-{novelId}` — 角色蓝图
- `last-read-{novelId}` — 最后阅读位置

---

## 7. 完整创作流程

### 7.1 从零到第一章

```
Step 1: 头脑风暴
  输入：无
  输出：10个选题（每个2句话，必须有悖论）
  工具：Dashboard 的 AI 建议

Step 2: 创建小说
  输入：书名 + 一句话简介 + 题材
  输出：novel记录 + 50个空章位
  工具：Dashboard 创建面板
  自动：中文标题→拼音ID，简介→AI扩写

Step 3: 世界观构建
  输入：选题 + 题材
  输出：完整世界观文档
  命令：按"1. 世界观构建"的Prompt生成
  产出：worldbuilding-{id}.md

Step 4: 写入世界观
  写入：WorldEditor页面 / PUT /api/novels/{id}/world
  字段：world_name, world_era, world_geo, power_system, main_arc, current_arc, world_rules

Step 5: 灵魂注入
  打开：SoulEngine（角色页面底部）
  选择：一组核心矛盾
  定位：1-10滑块
  写回答：你打算怎么追问这个矛盾
  保存：→ SQLite + localStorage

Step 6: 角色设计
  打开：CharacterSoul（角色页面顶部）
  设计：主角 + 反派 + 导师 + 搭档（至少4人）
  标准：金庸级（出场+标志+台词+创伤+弧线）
  保存：→ SQLite + localStorage

Step 7: 势力创建
  打开：WorldEditor → 势力标签页
  创建：4个势力（正方/反方/中立/灰色）
  保存：→ characters / factions 表

Step 8: 生成第1章
  设置：方向（可选）+ quality_threshold=0.8
  点击：生成下一章
  等待：约5-10分钟（12步管线）
  查看：GenerationStatusBanner 实时进度

Step 9: 判断第1章
  读完问自己：会不会点下一章？
  - YES → 继续第2章
  - NO  → 调整方向，重新生成
  不要微调！微调＜重写

Step 10: 批量生成
  点击：批量生成
  设置：count=20（最大）
  预计：约3-4小时（20章）
```

### 7.2 迭代判断标准

| 信号 | 判断 | 行动 |
|------|------|------|
| Q score ≥ 0.8 | 管线满意 | 保持参数 |
| Q score 0.65-0.8 | 管线勉强 | 降低threshold或加方向 |
| Q score < 0.65 | 管线不满意 | 检查provider/key/网络 |
| 读完不想点下一章 | 读者不满意 | 改方向重新生成 |
| 读完觉得"还行" | 危险信号 | "还行"=不会追读，重写 |
| 读完心跳加速 | 及格 | 继续 |

---

## 8. 番茄小说发布策略

### 8.1 发布前检查清单

```
□ 书名：3-7字，有动作/悬念/反差感
  好：我送外卖成神 / 我在三界送外卖
  差：一个外卖骑手的修仙之路 / 顾准传

□ 简介：前30字决定点击率，必须有具体画面
  好："凌晨三点，顾准接到一单外卖——配送地址是他自己的位置。"
  差："这是一个关于成长、爱与勇气的故事..."

□ 封面：对比色 + 大标题（番茄自动生成或自己上传）

□ 标签：精确匹配内容类型
  "都市" "异能" "系统流" "热血"

□ 前3章：每章结尾必须有钩子
  Ch1: 小满还有三分钟
  Ch2: 父母还活着
  Ch3: 应龙咬向亲爹

□ 章节字数：每章1800-3000字
  太少→推荐量低，太多→读完率低

□ 黄金三章：必须一起发布
  番茄首日推荐基于前3章读完率
```

### 8.2 发布节奏

```
第1天：发3章（黄金三章一次性放出）
第2-7天：每天2章（新书期流量扶持，多发多推）
第8-14天：每天1-2章（稳定期）
第15-30天：每天1章（保持日更不断更）
```

### 8.3 数据关注

```
核心指标：
- 读完率（单章）：<40%=严重问题，40-60%=一般，>60%=好
- 追读率（多章）：连续阅读3章以上的用户占比
- 书架加入率：加入书架/总阅读人数

每周检查：
- 哪章读完率骤降 → 那章有问题
- 哪章追读率上升 → 那章的钩子/情节有效
- 评论在说什么 → 用户真实反馈

读完率掉 → 加钩子（每章结尾必须有"然后呢？"）
追读率掉 → 加速节奏（减少日常/心理描写，推进主线）
```

---

## 9. 踩过的坑

### 9.1 API Key 相关

| 坑 | 现象 | 根因 | 修复 |
|----|------|------|------|
| DeepSeek Key 401 | 生成卡在20%不动 | provider_id默认"openai"，读了错误的key | 改provider_id="deepseek" |
| MiniMax Key 401 | 备选也失败 | Key过期 | 更新Key |
| Generator用错Key | CLI测试通过但服务端401 | Generator从DB读provider，但DB存的是旧key | 更新DB providers表 |
| 环境变量vs数据库不同步 | Settings改了Key但生成还报401 | Config读环境变量，Settings写数据库，两个不同源 | 统一从DB providers读 |

### 9.2 数据库相关

| 坑 | 现象 | 根因 | 修复 |
|----|------|------|------|
| 模块顶层实例化 | 双实例bug | `db = get_db()`在import时执行 | _LazyDB延迟代理 |
| 字段白名单缺失 | provider_id更新失败 | ALLOWED_NOVEL_COLS未包含provider_id | 加入白名单 |
| character_blueprints表缺失 | API返回no such table | 迁移代码在_init()中但表未创建 | 手动CREATE TABLE |
| 章节内容未保存 | 生成完成但DB为空 | add_chapter用了INSERT OR REPLACE但没有UNIQUE约束 | 确认(novel_id,number)有UNIQUE |

### 9.3 生成管线相关

| 坑 | 现象 | 根因 | 修复 |
|----|------|------|------|
| 评分通胀 | 所有章都A级 | judge_quality设了常量分(consistency=0.85) | 移除所有硬编码常量 |
| _humanize_text破坏结尾 | 章末钩子消失 | 把？！替换为。时未保护结尾 | 两阶段处理：分离结尾钩子→处理正文→还原钩子 |
| 备选模型base_url冲突 | 备选和主模型同一URL | _init_fallback只检查了base_url不同 | 增加provider ID检查 |
| model名不匹配 | gpt-4o-mini调用DeepSeek URL | fallback_client用default model名 | 从provider.models[0]读取正确model名 |

### 9.4 前端相关

| 坑 | 现象 | 根因 | 修复 |
|----|------|------|------|
| 生成进度不可见 | 用户不知道在生成 | 6px进度条在底部，完成3秒消失 | GenerationStatusBanner固定在内容区顶部 |
| 重复点击无反馈 | 点击后静默忽略 | 防重复点击guard没有toast | 加toast提示"正在生成中" |
| 双实例_gen_status | SSE和polling状态不同步 | 两份独立dict | GenerationState统一管理 |
| 引号独立成行 | 对话格式破碎 | AI短段落风格导致"引号单独一行 | 后处理合并孤立引号 |
| localStorage与DB不同步 | 角色在DB但前端看不到 | CharacterSoul只读localStorage | API优先加载+localStorage兜底 |

### 9.5 写小说相关

| 坑 | 现象 | 根因 | 经验 |
|----|------|------|------|
| AI版句句有比喻 | 读起来累 | AI默认"文学性=比喻密度" | 白描＞装饰。七分松弛三分紧 |
| 我自己写的太干 | 没画面感 | 另一个极端：只有动作没有感受 | 需要一个作家的手，不是工程师的手 |
| 微调不如重写 | 改来改去更差 | 局部修补破坏整体节奏 | 判断不过就重来，不要修修补补 |
| 50章空槽位 | 生成跳到第51章 | 创建小说时预建了50章但next_ch计算错误 | next_ch应过滤word_count>0的章节 |

---

## 附录：关键配置

```python
# config.py — 生成参数默认值
model: str = "deepseek-v4-pro"
temperature: float = 0.85
max_tokens_per_chapter: int = 4096
target_words_per_chapter: int = 2500
chapters_per_day: int = 1

# GenerateRequest — API请求参数
quality_threshold: float = 0.78  # 质量门控
direction: str = ""              # 方向指引
compression: str = "L1"          # 约束压缩等级(L0/L1/L2/L3/none)
soul_injection: str = ""         # 灵魂注入文本
```

---

> 这份文档记录的是建造灵墨的完整提示词体系。
> 核心原则：**每个环节一个Prompt，不扔给AI让它全自动。**
> 世界→灵魂→角色→生成→判断→迭代，六步闭环。
