# 灵墨 — 设计决策

## 决策 1：Web 控制台（CLI 保留为辅助）

**选了**：FastAPI 后端 + 纯前端 SPA（vanilla JS，无框架），CLI 保留。

**为什么**：
- 可视化小说列表、章节预览、一键操作——比敲命令直观
- 生成进度可以实时看到，不用 `tail -f` 日志
- FastAPI 天然支持 async + BackgroundTasks，适合长耗时生成任务
- 纯 vanilla JS 无框架依赖，体积极小，心智负担低

**代价**：比纯 CLI 多了 `server.py` + 前端文件。但结构清晰、组件分离，不增加后端核心逻辑的复杂度。

---

## 决策 2：SQLite 数据库

**选了**：SQLite 3，WAL 模式，13 张表，完整外键约束。

**为什么**：
- 数据模型天然是文档型（嵌套的角色、剧情、章节），JSON 映射直接
- 不需要 ORM、迁移、连接管理
- 单实例运行，无并发写冲突
- 文件可以直接用文本编辑器查看和修改

**代价**：比 JSON 文件多了 schema 管理。但单文件 .db 备份同样简单。

---

## 决策 3：Playwright 浏览器自动化发布

**选了**：Playwright + Chromium 模拟人类操作番茄小说后台。

**为什么**：番茄小说无公开创作 API。替代方案（逆向移动端 API）有强反爬。

**风险与应对**：
- 平台改版 → `smoke_test()` 前置检查
- 自动化检测 → 随机延迟 + 模拟人类打字
- 封号 → 平台抽象层，被封可换

---

## 决策 4：GPT-4o 主模型 + DeepSeek 备选

**选了**：OpenAI API 主引擎，DeepSeek fallback。

**为什么**：
- GPT-4o 中文长篇创作最佳
- DeepSeek 成本 ~1/10，兼容 OpenAI SDK

**每月成本（1本/天 1章）**：GPT-4o ¥11，DeepSeek ¥1。

---

## 决策 5：平台抽象基类

**选了**：`BasePlatform` ABC → 子类实现 3 个方法。

新平台接入成本 ~50 行。未来支持多平台并发。

---

## 决策 6：30 章状态重锚

每 30 章额外调一次 LLM 重新总结全局状态，防止长期漂移。

---

## 决策 7：质量检测

生成后自动检测字数、主角出场、重复度。在发布前拦截劣质内容。

---

## 决策 8：前端技术 — Vanilla JS SPA

**选了**：纯 HTML + CSS + vanilla JavaScript，hash 路由，零框架依赖。

**为什么**：
- 5 个页面、少量交互，不需要 React/Vue 的重量
- 体积极小（< 50KB），首次加载即用
- FastAPI `StaticFiles` 天然支持
- 无构建工具、无 node_modules、无 npm install

**代价**：复杂交互时（如富文本编辑器）需要更多手动 DOM 操作。当前范围不需要。

---

## 决策 9：显式不做的事

| 不做 | 理由 |
|------|------|
| 读者评论抓取 | 额外反爬 + NLP，ROI 低 |
| 封面 AI 生成 | V1 用平台默认封面 |
| 多人使用 / SaaS | 个人工具 |
| 自动签约 | 需实名 + 合同 |
| 数据库 | JSON 够用 |

---

## 决策 10：风险接受声明

- 平台封号 — 接受，可换平台
- 选择器失效 — 接受，有 smoke test 兜底
- 零读者/零收益 — 接受，V1 目标是跑通管线
- 长篇漂移 — 接受，有 30 章重锚缓解

## 未来升级路径

```
V1 (现在)        V2 (3 个月)        V3 (6 个月+)
─────────────────────────────────────────────────
Web 控制台        设计系统升级          SaaS 化
vanilla JS        htmx / Alpine        React/Vue
1 本小说          5 本并行            无上限
1 平台            3 平台              平台矩阵
JSON 文件         SQLite              云数据库
```
