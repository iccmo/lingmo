# Runtime dependencies — kept for Docker/CI backward compatibility.
# Canonical source: pyproject.toml [project.dependencies]
# For dev tools: pip install -e ".[dev]"
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
openai>=1.66.0
edge-tts>=6.1.0
ebooklib>=0.18
aiosqlite>=0.20.0
aiohttp>=3.10.0
Pillow>=10.0
